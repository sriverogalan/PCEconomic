version: "4.12"
services:
  node:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./quasar:/app
    ports:
      - 80:80
    expose:
      - 80
    command: sh -c "npm install && quasar dev"
    tty: true
